import {
    Card,
    CardHeader,
    CardContent,
} from '@material-ui/core/';

function CashFlowFilter(props) {

    return (
        <Card>
            <CardHeader
                title="Filtros"
            />
            <CardContent>
                <form>
                    
                </form>
            </CardContent>
        </Card>
    );
}

export default CashFlowFilter;